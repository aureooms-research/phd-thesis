\begin{contribution}[label=thm:abstract,restate=TheoremGPTAbstract]
  All abstract order types have an encoding
  using \(O(n^2)\) bits of space
  and allowing for queries in \(O(\log n)\) time.
  %\(O(n^2)\)-\(O(\log n)\)-encoding.
  %\(O(n^2)\)-bits \(O(\log n)\)-querytime encoding.
\end{contribution}
